pub mod query_patterns_metrics;

/// Registers every view provider in the views menu (F2).
pub fn register(context: &mut crate::interpreter::Context) {
    // Filled in as providers are ported.
    let _ = context;
}
